# Núcleo DB — PostgreSQL self-hosted (VPS)
# Formato: postgres://USER:PASSWORD@HOST:PORT/DATABASE
# Copie para .env.local (gitignored) e preencha. Na Vercel: Project Settings > Environment Variables.
DATABASE_URL=
